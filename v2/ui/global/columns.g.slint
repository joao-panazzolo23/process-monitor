import { AboutSlint, VerticalBox } from "std-widgets.slint";

export global Columns {
    out property <length> pid: 80px;
    out property <length> name: 400px;
    out property <length> cpu: 100px;
    out property <length> memory: 120px;
}
