pub struct SystemInfo {
    pub os_name: String,
    // pub os_version: String,
    pub kernel_version: String,
    pub memory_total_gb: f64,
    pub memory_used_gb: f64,
    pub cpus: f32,
    pub cpus_total_use: f32,
}
