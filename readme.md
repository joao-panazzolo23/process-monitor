# V1

run with

```
  cargo run -- <limit> <order-by> <interval> <filter-by>
```

for example:

```
  cargo run -- --limit 10 --order-by m --interval 10 --filter-by s
```

# V1

```
cargo run --release
```
